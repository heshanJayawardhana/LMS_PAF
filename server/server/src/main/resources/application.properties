spring.application.name=server
server.port=8080
spring.data.mongodb.uri=mongodb+srv://LmsPaf:lmspaf@lmspaf.gbiahbh.mongodb.net/smart_campus?retryWrites=true&w=majority
spring.mongodb.uri=mongodb+srv://LmsPaf:lmspaf@lmspaf.gbiahbh.mongodb.net/smart_campus?retryWrites=true&w=majority
spring.data.mongodb.auto-index-creation=true

# Frontend URL for OAuth redirect/CORS
app.frontend.url=${FRONTEND_URL:http://localhost:3000}

# Comma-separated email lists for role assignment after Google login
app.auth.admin-emails=${APP_ADMIN_EMAILS:admin@campus.edu}
app.auth.technician-emails=${APP_TECHNICIAN_EMAILS:tech@campus.edu}

# Google OAuth2 credentials
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
spring.security.oauth2.client.registration.google.scope=openid,profile,email